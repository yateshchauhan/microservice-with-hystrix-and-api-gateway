
server:
  port: 9090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8089/eureka

spring:
  application:
    name: Dashboard

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"